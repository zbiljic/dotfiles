# github.com/zbiljic/dotfiles

Managed using [chezmoi](https://github.com/twpayne/chezmoi).

## Install

### One-line install

```bash
sh -c "$(curl -fsLS get.chezmoi.io)" -- init --apply zbiljic
```

or

```bash
sh -c "$(wget -qO- get.chezmoi.io)" -- init --apply zbiljic
```
